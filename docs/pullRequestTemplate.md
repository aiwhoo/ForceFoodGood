##Description

I added the `operatingHours` property to the `RestaurantModel` class to allow users to see when a restaurant is open.

-Fixed Issue #83 (backend)

##Changes made
- Updated `RestaurantModel` constructor to accept `operatingHours`.
- Added `getOperatingHours()` and `setOperatingHours()` methods.

##Checklist
- My code follows OOP principles.
- There are no outstanding problems.
- I have reviewed the code myself.